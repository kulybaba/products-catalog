{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('app_default_index') }}" class="btn btn-secondary btn-sm">Back</a>
        <button class="btn btn-success btn-sm" type="submit">Registration</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
