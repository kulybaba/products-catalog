{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}
    <div class="row">
        <div class="card card-product mb-3">
            <div class="card-body p-3">
                <div class="row">
                    <div class="col">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <a href="{{ path('app_default_index') }}" class="btn btn-secondary btn-sm"><i class="fas fa-arrow-left"></i>&nbsp;Back</a>
                        <button class="btn btn-success btn-sm float-right" type="submit"><i class="fas fa-check"></i>&nbsp;Registration</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
