{% extends 'base.html.twig' %}

{% block title %}Edit comment{% endblock %}

{% block body %}
    <div class="row">
        <div class="card card-product">
            <div class="card-body p-3">
                <div class="row">
                    <div class="col">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <a href="{{ path('app_product_view', {'id': productId}) }}" class="btn btn-secondary btn-sm">Back</a>
                        <button class="btn btn-success btn-sm float-right" type="submit">Edit</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
