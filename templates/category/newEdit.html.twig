{% extends 'base.html.twig' %}

{% block title %}{{ title }} category{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('app_adminpanel_categorieslist') }}" class="btn btn-secondary btn-sm">Back</a>
        <button class="btn btn-success btn-sm" type="submit">{{ title }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
